# バックエンド配信停止

このリポジトリは画面配信用です。GASバックエンドをここから配信してはいけません。

- 正本: `hukusihoukoku_yokazu5/backend`
- 保存先の振り分け: 正本GASの `systemKey=yokazu6`
- `.clasp.json` と旧 `backend/Code.gs` は、保存先混在と旧版上書きを防ぐため削除済みです。

画面変更後も、バックエンドの更新は正本リポジトリだけで行ってください。
